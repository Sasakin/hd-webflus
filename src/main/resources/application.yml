spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: testdb
      username: userAdmin
      password: password


logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
    ROOT: ERROR